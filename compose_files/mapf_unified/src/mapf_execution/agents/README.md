Contains agents that implement the interface defined by ADGAgent.

shared_memory_dict_agent:  
Communicates via a SharedMemoryDict. For use with the mapf_pybullet simulation.

fiware_agent:  
Communicates via HTTP.
